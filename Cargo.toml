[package]
name = "raw_socket_capture"
version = "0.1.0"
edition = "2021"

[dependencies]
# 無任何依賴！

[profile.release]
opt-level = "z"     # 優化檔案大小
lto = true          # Link Time Optimization
codegen-units = 1   # 單一編譯單元
strip = true        # 移除符號
panic = "abort"     # 減小 panic 處理程式碼